// © 2023 27 Software
//
// ************************************************************************
// Author           : DXterity8 Version 8.6
// Created          : March 19, 2023 7:56:06 AM
// File             : EventHistoryStoredProcedureTestsBase.cs
// ************************************************************************

using System;
using System.Collections.Generic;
using System.Linq;
using System.Transactions;

using KnightsTour;
using KnightsTour.CoreLibrary;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace KnightsTourUnitTests.StoredProcedure
{
    /// <summary>
    /// Tests against the EventHistory entity stored procedures.  Inherits <seealso cref="BaseDataTest" />
    /// Generated On: March 19, 2023 at 7:56:06 AM by DXterity Solutions.
    /// Generated By: DXterity8 Version 8.6.0 (see https://dxteritysolutions.com/).
    /// </summary>
    /// <remarks>
    /// This class will be regenerated when requested to stay in sync with your model.
    /// </remarks>
    [TestClass]
    public class EventHistoryStoredProcedureTestsBase : DataTestBase<int?>
    {
        #region Declarations
        static SQLServerStorageHandler storageHandler = null; // A universal storage handler to support these stored procedure tests.
        #endregion Declarations

        #region Properties

        /// <summary>
        /// A universal storage handler to support these stored procedure tests.
        /// </summary>
        /// <value>
        /// The storage handler.
        /// </value>
        static SQLServerStorageHandler StorageHandler
        {
            get
            {
                if (storageHandler == null)
                {
                    storageHandler = (SQLServerStorageHandler)StorageProvider.GetHandler();
                }
                return storageHandler;
            }
        }
        #endregion Properties

        #region EventHistory stored procedure tests Methods

        /// <summary>
        /// Description for Event History_ Stored Procedure_ Create.
        /// </summary>
        [TestMethod]
        public void EventHistory_StoredProcedure_Create()
        {
            // Verify that the stored procedure exists.
            if (Enum.IsDefined(typeof(KnightsTour.Enumerations.StoredProcedure), "EventHistoryInsert"))
            {
                using (var transactionScope = new TransactionScope())
                {
                    // This creates a new, fully populated yet randomized entity.
                    KnightsTour.EventHistory eventHistory = GetNewEntity<KnightsTour.EventHistory>();

                    StorageStatement statement = new StorageStatement()
                    {
                        CommandType = System.Data.CommandType.StoredProcedure,
                        // In production, the StoredProcedureTransformation should be referenced by the StoredProcedure enumeration list, for example:.
                        // Statement = EntityMapper.StoredProcedureTransformation[KnightsTour.Enumerations.StoredProcedure.EventHistoryInsert.ToString()];
                        Statement = EntityMapper.StoredProcedureTransformation["EventHistoryInsert"],
                        Parameters = new List<IParameter>
                        {
                            new KnightsTour.CoreLibrary.GenericParameter("@EventTypeId", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.EventTypeId)),
                            new KnightsTour.CoreLibrary.GenericParameter("@SourceInternetAddress", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.SourceInternetAddress)),
                            new KnightsTour.CoreLibrary.GenericParameter("@Country", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.Country)),
                            new KnightsTour.CoreLibrary.GenericParameter("@Region", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.Region)),
                            new KnightsTour.CoreLibrary.GenericParameter("@City", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.City)),
                            new KnightsTour.CoreLibrary.GenericParameter("@ZipPostal", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.ZipPostal)),
                            new KnightsTour.CoreLibrary.GenericParameter("@Context", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.Context)),
                            new KnightsTour.CoreLibrary.GenericParameter("@MemberId", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.MemberId)),
                        }
                    };

                    try
                    {
                        int? pkValueReturned = StorageHandler.GetValue<int?>(statement);
                        Assert.IsNotNull(pkValueReturned);
                    }
                    catch (Exception exception)
                    {
                        if (IsKeyConstraintException(exception))
                        {
                            Assert.Inconclusive("Data constraint violated or invalid trigger code detected.");
                        }

                        Assert.Fail(exception.Message);
                    }
                } // Transaction scope.
            } // Enum defined.
            else
            {
                Assert.Inconclusive("The stored procedure 'EventHistoryInsert' could not be verified to exist.");
            }
        }

        /// <summary>
        /// Description for Event History_ Stored Procedure_ Update.
        /// </summary>
        [TestMethod]
        public void EventHistory_StoredProcedure_Update()
        {
            // Verify that the stored procedure exists.
            if (Enum.IsDefined(typeof(KnightsTour.Enumerations.StoredProcedure), "EventHistoryUpdate"))
            {
                using (var transactionScope = new TransactionScope())
                {
                    #region Data validation
                    long totalRecords = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
                    int? idToSelect = null;

                    // This creates a new, fully populated yet randomized entity.
                    KnightsTour.EventHistory eventHistory = GetNewEntity<KnightsTour.EventHistory>();

                    // Insert a new record if required to validate this test.
                    if (totalRecords == 0)
                    {
                        try
                        {
                            IActionResponse insertResponse = new EventHistoryLogic("Unit test").Insert(eventHistory);

                            if (!insertResponse.IsValid)
                            {
                                Assert.Fail(insertResponse.Messages.Where(m => m.Type == KnightsTour.CoreLibrary.Enumerations.MessageType.Negative).First().Content);
                            }
                        }
                        catch (Exception exception)
                        {
                            if (IsKeyConstraintException(exception))
                            {
                                Assert.Inconclusive("Unit test passed, unique data constraints check kicked in and verified");
                            }

                            Assert.Fail(exception.Message);
                        }
                    }
                    else
                    {
                        idToSelect = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.FirstId);
                        eventHistory = new EventHistoryLogic("Unit test").GetById(idToSelect);
                    }
                    #endregion

                    #region Dynamically update the newly created eventHistory
                    if (GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasStringField))
                    {
                        string stringField = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.StringField);
                        string currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.EventHistory, string>(eventHistory, stringField);
                        string newValue = "modified";
                        if (!string.IsNullOrEmpty(currentValue))
                        {
                            newValue = currentValue + " modified.";
                            if (currentValue.StartsWith("a"))
                            {
                                newValue = currentValue.Replace("a", "z");
                            }
                        }
                        KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.EventHistory>(eventHistory, stringField, newValue);
                    }
                    else if (GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasIntField))
                    {
                        string intField = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.IntField);
                        int currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.EventHistory, int>(eventHistory, intField);
                        int newValue = currentValue + 1;
                        KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.EventHistory>(eventHistory, intField, newValue);
                    }
                    #endregion

                    // Update the object using the stored procedure.
                    StorageStatement statement = new StorageStatement()
                    {
                        CommandType = System.Data.CommandType.StoredProcedure,
                        // In production, the StoredProcedureTransformation should be referenced by the StoredProcedure enumeration list, for example:.
                        // Statement = EntityMapper.StoredProcedureTransformation[KnightsTour.Enumerations.StoredProcedure.EventHistoryUpdate.ToString()];
                        Statement = EntityMapper.StoredProcedureTransformation["EventHistoryUpdate"],
                        Parameters = new List<IParameter>
                        {
                            new KnightsTour.CoreLibrary.GenericParameter("@EventHistoryId", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.EventHistoryId)),
                            new KnightsTour.CoreLibrary.GenericParameter("@EventTypeId", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.EventTypeId)),
                            new KnightsTour.CoreLibrary.GenericParameter("@EventDate", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.EventDate)),
                            new KnightsTour.CoreLibrary.GenericParameter("@SourceInternetAddress", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.SourceInternetAddress)),
                            new KnightsTour.CoreLibrary.GenericParameter("@Country", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.Country)),
                            new KnightsTour.CoreLibrary.GenericParameter("@Region", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.Region)),
                            new KnightsTour.CoreLibrary.GenericParameter("@City", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.City)),
                            new KnightsTour.CoreLibrary.GenericParameter("@ZipPostal", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.ZipPostal)),
                            new KnightsTour.CoreLibrary.GenericParameter("@Context", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.Context)),
                            new KnightsTour.CoreLibrary.GenericParameter("@MemberId", eventHistory.GetDBValue(KnightsTour.Enumerations.EventHistoryProperty.MemberId)),
                        }
                    };

                    try
                    {
                        int recordsUpdated = StorageHandler.Execute(statement);

                        // Validate the record was in fact updated.
                        Assert.AreEqual(recordsUpdated, 1);
                    }
                    catch (Exception exception)
                    {
                        if (IsKeyConstraintException(exception))
                        {
                            Assert.Inconclusive("Data constraint violated or invalid trigger code detected.");
                        }

                        Assert.Fail(exception.Message);
                    }
                } // Transaction scope.
            } // Enum defined.
            else
            {
                Assert.Inconclusive("The stored procedure 'EventHistoryUpdate' could not be verified to exist.");
            }
        }

        /// <summary>
        /// Description for Event History_ Stored Procedure_ Get By Id.
        /// </summary>
        [TestMethod]
        public void EventHistory_StoredProcedure_GetById()
        {
            // Verify that the stored procedure exists.
            if (Enum.IsDefined(typeof(KnightsTour.Enumerations.StoredProcedure), "EventHistoryGetById"))
            {
                using (var transactionScope = new TransactionScope())
                {
                    #region Data validation
                    long totalRecords = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
                    int? idToSelect = null;

                    // Insert a new record if required to validate this test.
                    if (totalRecords == 0)
                    {
                        // This creates a new, fully populated yet randomized entity.
                        KnightsTour.EventHistory eventHistory = GetNewEntity<KnightsTour.EventHistory>();

                        try
                        {
                            IActionResponse insertResponse = new EventHistoryLogic("Unit test").Insert(eventHistory);

                            if (!insertResponse.IsValid)
                            {
                                Assert.Fail(insertResponse.Messages.Where(m => m.Type == KnightsTour.CoreLibrary.Enumerations.MessageType.Negative).First().Content);
                            }
                            else
                            {
                                totalRecords = 1;
                                idToSelect = eventHistory.EventHistoryId;
                            }
                        }
                        catch (Exception exception)
                        {
                            if (IsKeyConstraintException(exception))
                            {
                                Assert.Inconclusive("Data constraint violated or invalid trigger code detected.");
                            }

                            Assert.Fail(exception.Message);
                        }
                    }
                    else
                    {
                        idToSelect = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.FirstId);
                    }
                    #endregion

                    StorageStatement statement = new StorageStatement()
                    {
                        CommandType = System.Data.CommandType.StoredProcedure,
                        // In production, the StoredProcedureTransformation should be referenced by the StoredProcedure enumeration list, for example:.
                        // Statement = EntityMapper.StoredProcedureTransformation[KnightsTour.Enumerations.StoredProcedure.EventHistoryGetById.ToString()];
                        Statement = EntityMapper.StoredProcedureTransformation["EventHistoryGetById"],
                        Parameter = new KnightsTour.CoreLibrary.GenericParameter("@EventHistoryId", idToSelect),
                    };

                    KnightsTour.EventHistory record = new KnightsTour.EventHistory(StorageHandler.GetRecord(statement));

                    // Validate the record pk returned matches the expected identifier.
                    Assert.AreEqual(record.EventHistoryId, idToSelect);
                }
            }
            else
            {
                Assert.Inconclusive("The stored procedure 'EventHistoryGetById' could not be verified to exist.");
            }
        }

        /// <summary>
        /// Description for Event History_ Stored Procedure_ Get All.
        /// </summary>
        [TestMethod]
        public void EventHistory_StoredProcedure_GetAll()
        {
            // Verify that the stored procedure exists.
            if (Enum.IsDefined(typeof(KnightsTour.Enumerations.StoredProcedure), "EventHistoryGetAll"))
            {
                using (var transactionScope = new TransactionScope())
                {
                    #region Data validation
                    long totalRecords = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);

                    // Insert a new record if required to validate this test.
                    if (totalRecords == 0)
                    {
                        // This creates a new, fully populated yet randomized entity.
                        KnightsTour.EventHistory eventHistory = GetNewEntity<KnightsTour.EventHistory>();

                        try
                        {
                            IActionResponse insertResponse = new EventHistoryLogic("Unit test").Insert(eventHistory);

                            if (!insertResponse.IsValid)
                            {
                                Assert.Fail(insertResponse.Messages.Where(m => m.Type == KnightsTour.CoreLibrary.Enumerations.MessageType.Negative).First().Content);
                            }
                            else
                            {
                                totalRecords = 1;
                            }
                        }
                        catch (Exception exception)
                        {
                            if (IsKeyConstraintException(exception))
                            {
                                Assert.Inconclusive("Data constraint violated or invalid trigger code detected.");
                            }

                            Assert.Fail(exception.Message);
                        }
                    }
                    #endregion

                    StorageStatement statement = new StorageStatement()
                    {
                        CommandType = System.Data.CommandType.StoredProcedure,
                        // In production, the StoredProcedureTransformation should be referenced by the StoredProcedure enumeration list, for example:.
                        // Statement = EntityMapper.StoredProcedureTransformation[KnightsTour.Enumerations.StoredProcedure.EventHistoryGetAll.ToString()];
                        Statement = EntityMapper.StoredProcedureTransformation["EventHistoryGetAll"],
                    };

                    List<KnightsTour.EventHistory> records = new List<KnightsTour.EventHistory>();
                    foreach (dynamic dataRow in StorageHandler.GetRecords(statement))
                    {
                        records.Add(new KnightsTour.EventHistory(dataRow));
                    }

                    // Validate the total number of records returned match the total number of rows known to exist.
                    Assert.AreEqual(totalRecords, records.Count);
                }
            }
            else
            {
                Assert.Inconclusive("The stored procedure 'EventHistoryGetAll' could not be verified to exist.");
            }
        }

        /// <summary>
        /// Description for Event History_ Stored Procedure_ Delete.
        /// </summary>
        [TestMethod]
        public void EventHistory_StoredProcedure_Delete()
        {
            // Verify that the stored procedure exists.
            if (Enum.IsDefined(typeof(KnightsTour.Enumerations.StoredProcedure), "EventHistoryDelete"))
            {
                using (var transactionScope = new TransactionScope())
                {
                    #region Data validation
                    long totalRecords = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
                    int? idToDelete = null;

                    // Insert a new record if required to validate this test.
                    if (totalRecords == 0)
                    {
                        // This creates a new, fully populated yet randomized entity.
                        KnightsTour.EventHistory eventHistory = GetNewEntity<KnightsTour.EventHistory>();

                        try
                        {
                            IActionResponse insertResponse = new EventHistoryLogic("Unit test").Insert(eventHistory);

                            if (!insertResponse.IsValid)
                            {
                                Assert.Fail(insertResponse.Messages.Where(m => m.Type == KnightsTour.CoreLibrary.Enumerations.MessageType.Negative).First().Content);
                            }
                            else
                            {
                                totalRecords = 1;
                                idToDelete = eventHistory.EventHistoryId;
                            }
                        }
                        catch (Exception exception)
                        {
                            if (IsKeyConstraintException(exception))
                            {
                                Assert.Inconclusive("Unit test passed, unique data constraints check kicked in and verified");
                            }

                            Assert.Fail(exception.Message);
                        }
                    }
                    else
                    {
                        idToDelete = GetEntityProperty<KnightsTour.EventHistory, KnightsTour.EventHistoryLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.FirstId);
                    }
                    #endregion

                    StorageStatement statement = new StorageStatement()
                    {
                        CommandType = System.Data.CommandType.StoredProcedure,
                        // In production, the StoredProcedureTransformation should be referenced by the StoredProcedure enumeration list, for example:.
                        // Statement = EntityMapper.StoredProcedureTransformation[KnightsTour.Enumerations.StoredProcedure.EventHistoryDelete.ToString()];
                        Statement = EntityMapper.StoredProcedureTransformation["EventHistoryDelete"],
                        Parameter = new KnightsTour.CoreLibrary.GenericParameter("@EventHistoryId", idToDelete),
                    };

                    try
                    {
                        int recordsDeleted = StorageHandler.Execute(statement);

                        // Validate the record was in fact deleted.
                        Assert.AreEqual(recordsDeleted, 1);
                    }
                    catch (Exception exception)
                    {
                        if (IsKeyConstraintException(exception))
                        {
                            Assert.Inconclusive("Data constraint violated or invalid trigger code detected.");
                        }
                    }
                }
            }
            else
            {
                Assert.Inconclusive("The stored procedure 'EventHistoryDelete' could not be verified to exist.");
            }
        }
        #endregion EventHistory stored procedure tests Methods

    } // Class
} // Namespace