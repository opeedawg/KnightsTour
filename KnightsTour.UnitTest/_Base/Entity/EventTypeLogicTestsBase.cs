// © 2023 27 Software
//
// ************************************************************************
// Author           : DXterity8 Version 8.6
// Created          : March 19, 2023 7:56:06 AM
// File             : EventTypeLogicTestsBase.cs
// ************************************************************************

using System;
using System.Collections.Generic;
using System.Linq;
using System.Transactions;

using KnightsTour;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace KnightsTourUnitTests.Entity
{
    /// <summary>
    /// Tests against the EventTypeLogic support class.  Inherits <seealso cref="BaseDataTest" />
    /// Generated On: March 19, 2023 at 7:56:06 AM by DXterity Solutions.
    /// Generated By: DXterity8 Version 8.6.0 (see https://dxteritysolutions.com/).
    /// </summary>
    /// <remarks>
    /// This class will be regenerated when requested to stay in sync with your model.
    /// </remarks>
    [TestClass]
    public class EventTypeLogicTestsBase : DataTestBase<int?>
    {
        #region EventType logic tests Methods

        /// <summary>
        /// Description for Event Type Logic_ Has Repository.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_HasRepository()
        {
            KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
            KnightsTour.CoreLibrary.IRepository<KnightsTour.EventType, KnightsTour.EventTypeLite, int?> repository = logic.Repository;
            Assert.IsNotNull(repository);
        }

        /// <summary>
        /// Description for Event Type Logic_ Has Storage Handler.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_HasStorageHandler()
        {
            KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
            KnightsTour.CoreLibrary.IStorageHandler handler = logic.StorageHandler;
            Assert.IsNotNull(handler);
        }

        /// <summary>
        /// Description for Event Type Logic_ Get By Id.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_GetById()
        {
            EntityRepository_GetById<KnightsTour.EventType, KnightsTour.EventTypeLite>();
        }

        /// <summary>
        /// Description for Event Type Logic_ Get By Ids.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_GetByIds()
        {
            KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
            if (GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount) > 1)
            {
                int? firstId = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.FirstId);
                int? lastId = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.LastId);
                List<int?> ids = new List<int?>() { firstId, lastId };

                List<KnightsTour.EventType> eventTypes = logic.GetByIds(ids).ToList();
                Assert.IsNotNull(eventTypes);
                Assert.AreEqual(eventTypes.Count, 2);
                eventTypes = eventTypes.OrderBy(x => x.Id).ToList();
                Assert.AreEqual(eventTypes[0].Id, firstId);
                Assert.AreEqual(eventTypes[1].Id, lastId);
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Get All.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_GetAll()
        {
            KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
            long totalRecords = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
            if (totalRecords > 0)
            {
                List<KnightsTour.EventType> eventTypes = logic.GetAll().ToList();
                Assert.IsTrue(eventTypes.Count > 0);
                Assert.IsTrue(eventTypes.Count >= totalRecords);
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Get All_ By Condition.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_GetAll_ByCondition()
        {
            KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
            string pk = new KnightsTour.EventType().PrimaryKeyField;

            long totalRecords = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
            if (totalRecords > 1)
            {
                int? maxId = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.LastId);
                List<KnightsTour.EventType> eventTypes = logic.GetAll(new KnightsTour.CoreLibrary.RetrievalCondition { SQLWhere = $"{StorageProvider.GetColumnSQL(pk)} < {maxId}" }).ToList();
                Assert.IsTrue(eventTypes.Count > 0);
                Assert.IsTrue(eventTypes.Count < totalRecords);
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Get All_ By Predicate.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_GetAll_ByPredicate()
        {
            long totalRecords = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
            if (totalRecords > 0)
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
                int? maxId = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.LastId);
                EntityRepository_GetAll_ByPredicate<KnightsTour.EventType, KnightsTour.EventTypeLite>(a => a.EventTypeId < maxId);

                if (totalRecords > 1)
                {
                    List<KnightsTour.EventType> eventTypes = logic.GetAll(a => a.EventTypeId < maxId).ToList();
                    Assert.IsTrue(eventTypes.Count > 0);
                    Assert.IsTrue(eventTypes.Count < totalRecords);
                }
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Get All_ By F K.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_GetAll_ByFK()
        {
            KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
            foreach (string property in EntityMapper.FKNames(new KnightsTour.EventType().EntityName))
            {
                // Get some valid FK Values.
                foreach (int? fkValue in StorageProvider.GetFKValues<KnightsTour.EventType, KnightsTour.EventTypeLite, int?>(property))
                {
                    List<KnightsTour.EventType> eventTypes = logic.GetByFK(property, fkValue, new KnightsTour.CoreLibrary.RetrievalCondition { SQLOrderBy = property, Skip = 0, Take = MaxFKQuantity }).ToList();
                    Assert.IsTrue(eventTypes.Count > 0);
                    Assert.IsTrue(eventTypes.Count <= MaxFKQuantity);
                }
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Get All_ By F Ks.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_GetAll_ByFKs()
        {
            KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
            foreach (string property in EntityMapper.FKNames(new KnightsTour.EventType().EntityName))
            {
                // Get some valid FK Values.
                List<KnightsTour.EventType> eventTypes = logic.GetByFKs(property, KnightsTour.StorageProvider.GetFKValues<KnightsTour.EventType, KnightsTour.EventTypeLite, int?>(property), new KnightsTour.CoreLibrary.RetrievalCondition { SQLOrderBy = property, Skip = 0, Take = MaxFKQuantity }).ToList();
                Assert.IsTrue(eventTypes.Count > 0);
                Assert.IsTrue(eventTypes.Count <= MaxFKQuantity);
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Insert_ Single.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_Insert_Single()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventType eventType = InsertEntity<KnightsTour.EventType, KnightsTour.EventTypeLite>();
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Insert_ Multiple.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_Insert_Multiple()
        {
            // Block size larger than the number of accounts.
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                List<KnightsTour.EventType> eventTypes = CreateEntities<KnightsTour.EventType>(BulkTestQuantity);

                Assert.IsNotNull(eventTypes);
                Assert.IsTrue(eventTypes.Count == BulkTestQuantity);
                foreach (KnightsTour.EventType eventType in eventTypes)
                {
                    Assert.IsTrue(eventType.IsNew);
                }

                // If the primary key insertion type is manual, then configure some values now.
                if (eventTypes[0].PKInsertConfiguration == KnightsTour.CoreLibrary.Enumerations.InsertPKRule.Manual)
                {
                    foreach (KnightsTour.EventType eventType in eventTypes)
                    {
                        eventType.Id = GetRandomValue<int?>();
                    }
                }

                KnightsTour.CoreLibrary.IActionResponse response = logic.Insert(eventTypes, 500);
                Assert.IsTrue(response.IsValid);

                // Block size less than the number of accounts.
                eventTypes = CreateEntities<KnightsTour.EventType>(BulkTestQuantity);

                Assert.IsNotNull(eventTypes);
                Assert.IsTrue(eventTypes.Count == BulkTestQuantity);
                foreach (KnightsTour.EventType eventType in eventTypes)
                {
                    Assert.IsTrue(eventType.IsNew);
                }

                // If the primary key insertion type is manual, then configure some values now.
                if (eventTypes[0].PKInsertConfiguration == KnightsTour.CoreLibrary.Enumerations.InsertPKRule.Manual)
                {
                    foreach (KnightsTour.EventType eventType in eventTypes)
                    {
                        eventType.Id = GetRandomValue<int?>();
                    }
                }

                response = logic.Insert(eventTypes, BulkTestQuantity - 1);
                Assert.IsTrue(response.IsValid);
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Update_ Single.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_Update_Single()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                #region Create and insert a new eventType
                KnightsTour.EventType eventType = InsertEntity<KnightsTour.EventType, KnightsTour.EventTypeLite>();
                #endregion

                #region Now let's update that newly created eventType
                if (GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasStringField))
                {
                    string stringField = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.StringField);
                    string currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.EventType, string>(eventType, stringField);
                    string newValue = "modified";
                    if (string.IsNullOrEmpty(currentValue))
                    {
                        newValue = currentValue + " modified.";
                        if (currentValue.StartsWith("a"))
                        {
                            newValue = currentValue.Replace("a", "z");
                        }
                    }
                    KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.EventType>(eventType, stringField, newValue);
                    KnightsTour.CoreLibrary.IActionResponse updateResponse = logic.Update(eventType);
                    Assert.IsTrue(updateResponse.IsValid);
                }
                else if (GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasIntField))
                {
                    string intField = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.IntField);
                    int currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.EventType, int>(eventType, intField);
                    int newValue = currentValue + 1;
                    KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.EventType>(eventType, intField, newValue);
                    KnightsTour.CoreLibrary.IActionResponse updateResponse = logic.Update(eventType);
                    Assert.IsTrue(updateResponse.IsValid);
                }
                #endregion
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Update_ Multiple.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_Update_Multiple()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                string pk = new KnightsTour.EventType().PrimaryKeyField;

                if (GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount) > 0)
                {
                    List<KnightsTour.EventType> eventTypes = logic.GetAll(new KnightsTour.CoreLibrary.RetrievalCondition
                    {
                        SQLOrderBy = StorageProvider.GetColumnSQL(pk),
                        Skip = 0,
                        Take = BulkTestQuantity
                    }).ToList();

                    Assert.IsTrue(eventTypes.Count > 0);

                    // Modify each account.
                    bool modified = false;
                    foreach (KnightsTour.EventType eventType in eventTypes)
                    {
                        if (GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasStringField))
                        {
                            string stringField = GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.StringField);
                            string currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.EventType, string>(eventType, stringField);
                            string newValue = "modified";
                            if (string.IsNullOrEmpty(currentValue))
                            {
                                newValue = currentValue + " modified.";
                                if (currentValue.StartsWith("a"))
                                {
                                    newValue = currentValue.Replace("a", "z");
                                }
                            }
                            KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.EventType>(eventType, stringField, newValue);
                            modified = true;
                        }
                        else if (GetEntityProperty<KnightsTour.EventType, KnightsTour.EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasIntField))
                        {
                            string intField = GetEntityProperty<KnightsTour.EventType, EventTypeLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.IntField);
                            int currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.EventType, int>(eventType, intField);
                            int newValue = currentValue + 1;
                            KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.EventType>(eventType, intField, newValue);
                            modified = true;
                        }
                    }
                    if (modified)
                    {
                        KnightsTour.CoreLibrary.IActionResponse updateResponse = logic.Update(eventTypes);
                        Assert.IsTrue(updateResponse.IsValid);
                    }
                }
            }

        }

        /// <summary>
        /// Description for Event Type Logic_ Delete By Id.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_DeleteById()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                #region Create and insert a new eventType
                KnightsTour.EventType eventType = InsertEntity<KnightsTour.EventType, KnightsTour.EventTypeLite>();
                #endregion

                #region Now let's delete that newly created eventType by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(eventType.Id.Value);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Delete By Ids.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_DeleteByIds()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                #region Create and insert some new eventTypes
                List<int?> newIds = new List<int?>();
                for (int i = 0; i < BulkTestQuantity; i++)
                {
                    KnightsTour.EventType eventType = InsertEntity<KnightsTour.EventType, KnightsTour.EventTypeLite>();
                    newIds.Add(eventType.Id.Value);
                }
                Assert.IsTrue(newIds.Count == BulkTestQuantity);
                #endregion

                #region Now let's delete that newly cretaed eventType by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(newIds);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Delete By Entity.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_DeleteByEntity()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                #region Create and insert a new eventType
                KnightsTour.EventType eventType = InsertEntity<KnightsTour.EventType, KnightsTour.EventTypeLite>();
                #endregion

                #region Now let's delete that newly created eventType by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(eventType);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Delete By Entities.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_DeleteByEntities()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                #region Create and insert some new eventTypes
                List<KnightsTour.EventType> newEntities = new List<KnightsTour.EventType>();
                for (int i = 0; i < BulkTestQuantity; i++)
                {
                    KnightsTour.EventType eventType = InsertEntity<KnightsTour.EventType, KnightsTour.EventTypeLite>();
                    newEntities.Add(eventType);
                }
                Assert.IsTrue(newEntities.Count == BulkTestQuantity);
                #endregion

                #region Now let's delete that newly created eventType by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(newEntities);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Delete_ Cascade.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_Delete_Cascade()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);

                #region Create and insert a new eventType
                KnightsTour.EventType eventType = InsertEntity<KnightsTour.EventType, KnightsTour.EventTypeLite>();
                #endregion

                #region Now let's delete that newly created eventType by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.DeleteCascade(eventType.Id.Value);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Event Type Logic_ Delete_ By F K.
        /// </summary>
        [TestMethod]
        public void EventTypeLogic_Delete_ByFK()
        {
            EntityMetaData meta = new EntityMetaData(KnightsTour.Enumerations.EntityName.EventType, UserName);

            if (meta.Dependencies.Count > 0)
            {
                Assert.Inconclusive("Delete by FK is not possible due to dependencies.");
            }
            else
            {
                KnightsTour.EventTypeLogic logic = new KnightsTour.EventTypeLogic(UserName);
                using (var transactionScope = new TransactionScope())
                {
                    foreach (string property in EntityMapper.FKNames(new KnightsTour.EventType().EntityName))
                    {
                        // Get some valid FK Values.
                        foreach (int fkValue in StorageProvider.GetFKValues<KnightsTour.EventType, KnightsTour.EventTypeLite, int?>(property))
                        {
                            KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.DeleteByFK(property, fkValue);
                            Assert.IsTrue(deleteResponse.IsValid);
                        }
                    }
                }
            }
        }
        #endregion EventType logic tests Methods

    } // Class
} // Namespace