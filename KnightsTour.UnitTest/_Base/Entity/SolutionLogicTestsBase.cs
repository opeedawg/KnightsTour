// © 2023 27 Software
//
// ************************************************************************
// Author           : DXterity8 Version 8.6
// Created          : March 19, 2023 7:56:06 AM
// File             : SolutionLogicTestsBase.cs
// ************************************************************************

using System;
using System.Collections.Generic;
using System.Linq;
using System.Transactions;

using KnightsTour;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace KnightsTourUnitTests.Entity
{
    /// <summary>
    /// Tests against the SolutionLogic support class.  Inherits <seealso cref="BaseDataTest" />
    /// Generated On: March 19, 2023 at 7:56:06 AM by DXterity Solutions.
    /// Generated By: DXterity8 Version 8.6.0 (see https://dxteritysolutions.com/).
    /// </summary>
    /// <remarks>
    /// This class will be regenerated when requested to stay in sync with your model.
    /// </remarks>
    [TestClass]
    public class SolutionLogicTestsBase : DataTestBase<int?>
    {
        #region Solution logic tests Methods

        /// <summary>
        /// Description for Solution Logic_ Has Repository.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_HasRepository()
        {
            KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
            KnightsTour.CoreLibrary.IRepository<KnightsTour.Solution, KnightsTour.SolutionLite, int?> repository = logic.Repository;
            Assert.IsNotNull(repository);
        }

        /// <summary>
        /// Description for Solution Logic_ Has Storage Handler.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_HasStorageHandler()
        {
            KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
            KnightsTour.CoreLibrary.IStorageHandler handler = logic.StorageHandler;
            Assert.IsNotNull(handler);
        }

        /// <summary>
        /// Description for Solution Logic_ Get By Id.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_GetById()
        {
            EntityRepository_GetById<KnightsTour.Solution, KnightsTour.SolutionLite>();
        }

        /// <summary>
        /// Description for Solution Logic_ Get By Ids.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_GetByIds()
        {
            KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
            if (GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount) > 1)
            {
                int? firstId = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.FirstId);
                int? lastId = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.LastId);
                List<int?> ids = new List<int?>() { firstId, lastId };

                List<KnightsTour.Solution> solutions = logic.GetByIds(ids).ToList();
                Assert.IsNotNull(solutions);
                Assert.AreEqual(solutions.Count, 2);
                solutions = solutions.OrderBy(x => x.Id).ToList();
                Assert.AreEqual(solutions[0].Id, firstId);
                Assert.AreEqual(solutions[1].Id, lastId);
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Get All.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_GetAll()
        {
            KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
            long totalRecords = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
            if (totalRecords > 0)
            {
                List<KnightsTour.Solution> solutions = logic.GetAll().ToList();
                Assert.IsTrue(solutions.Count > 0);
                Assert.IsTrue(solutions.Count >= totalRecords);
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Get All_ By Condition.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_GetAll_ByCondition()
        {
            KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
            string pk = new KnightsTour.Solution().PrimaryKeyField;

            long totalRecords = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
            if (totalRecords > 1)
            {
                int? maxId = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.LastId);
                List<KnightsTour.Solution> solutions = logic.GetAll(new KnightsTour.CoreLibrary.RetrievalCondition { SQLWhere = $"{StorageProvider.GetColumnSQL(pk)} < {maxId}" }).ToList();
                Assert.IsTrue(solutions.Count > 0);
                Assert.IsTrue(solutions.Count < totalRecords);
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Get All_ By Predicate.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_GetAll_ByPredicate()
        {
            long totalRecords = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount);
            if (totalRecords > 0)
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
                int? maxId = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.LastId);
                EntityRepository_GetAll_ByPredicate<KnightsTour.Solution, KnightsTour.SolutionLite>(a => a.SolutionId < maxId);

                if (totalRecords > 1)
                {
                    List<KnightsTour.Solution> solutions = logic.GetAll(a => a.SolutionId < maxId).ToList();
                    Assert.IsTrue(solutions.Count > 0);
                    Assert.IsTrue(solutions.Count < totalRecords);
                }
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Get All_ By F K.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_GetAll_ByFK()
        {
            KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
            foreach (string property in EntityMapper.FKNames(new KnightsTour.Solution().EntityName))
            {
                // Get some valid FK Values.
                foreach (int? fkValue in StorageProvider.GetFKValues<KnightsTour.Solution, KnightsTour.SolutionLite, int?>(property))
                {
                    List<KnightsTour.Solution> solutions = logic.GetByFK(property, fkValue, new KnightsTour.CoreLibrary.RetrievalCondition { SQLOrderBy = property, Skip = 0, Take = MaxFKQuantity }).ToList();
                    Assert.IsTrue(solutions.Count > 0);
                    Assert.IsTrue(solutions.Count <= MaxFKQuantity);
                }
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Get All_ By F Ks.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_GetAll_ByFKs()
        {
            if(StorageProvider.GetEntityAttribute<KnightsTour.Solution, KnightsTour.SolutionLite, int?>().RecordCount == 0)
            {
                Assert.Inconclusive("Unable to insert an entity of type 'solution' due to lack of data.");
            }

            KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
            foreach (string property in EntityMapper.FKNames(new KnightsTour.Solution().EntityName))
            {
                // Get some valid FK Values.
                List<KnightsTour.Solution> solutions = logic.GetByFKs(property, KnightsTour.StorageProvider.GetFKValues<KnightsTour.Solution, KnightsTour.SolutionLite, int?>(property), new KnightsTour.CoreLibrary.RetrievalCondition { SQLOrderBy = property, Skip = 0, Take = MaxFKQuantity }).ToList();
                Assert.IsTrue(solutions.Count > 0);
                Assert.IsTrue(solutions.Count <= MaxFKQuantity);
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Insert_ Single.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_Insert_Single()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.Solution solution = InsertEntity<KnightsTour.Solution, KnightsTour.SolutionLite>();
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Insert_ Multiple.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_Insert_Multiple()
        {
            // Block size larger than the number of accounts.
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                List<KnightsTour.Solution> solutions = CreateEntities<KnightsTour.Solution>(BulkTestQuantity);

                Assert.IsNotNull(solutions);
                Assert.IsTrue(solutions.Count == BulkTestQuantity);
                foreach (KnightsTour.Solution solution in solutions)
                {
                    Assert.IsTrue(solution.IsNew);
                }

                // If the primary key insertion type is manual, then configure some values now.
                if (solutions[0].PKInsertConfiguration == KnightsTour.CoreLibrary.Enumerations.InsertPKRule.Manual)
                {
                    foreach (KnightsTour.Solution solution in solutions)
                    {
                        solution.Id = GetRandomValue<int?>();
                    }
                }

                KnightsTour.CoreLibrary.IActionResponse response = logic.Insert(solutions, 500);
                Assert.IsTrue(response.IsValid);

                // Block size less than the number of accounts.
                solutions = CreateEntities<KnightsTour.Solution>(BulkTestQuantity);

                Assert.IsNotNull(solutions);
                Assert.IsTrue(solutions.Count == BulkTestQuantity);
                foreach (KnightsTour.Solution solution in solutions)
                {
                    Assert.IsTrue(solution.IsNew);
                }

                // If the primary key insertion type is manual, then configure some values now.
                if (solutions[0].PKInsertConfiguration == KnightsTour.CoreLibrary.Enumerations.InsertPKRule.Manual)
                {
                    foreach (KnightsTour.Solution solution in solutions)
                    {
                        solution.Id = GetRandomValue<int?>();
                    }
                }

                response = logic.Insert(solutions, BulkTestQuantity - 1);
                Assert.IsTrue(response.IsValid);
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Update_ Single.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_Update_Single()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                #region Create and insert a new solution
                KnightsTour.Solution solution = InsertEntity<KnightsTour.Solution, KnightsTour.SolutionLite>();
                #endregion

                #region Now let's update that newly created solution
                if (GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasStringField))
                {
                    string stringField = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.StringField);
                    string currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.Solution, string>(solution, stringField);
                    string newValue = "modified";
                    if (string.IsNullOrEmpty(currentValue))
                    {
                        newValue = currentValue + " modified.";
                        if (currentValue.StartsWith("a"))
                        {
                            newValue = currentValue.Replace("a", "z");
                        }
                    }
                    KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.Solution>(solution, stringField, newValue);
                    KnightsTour.CoreLibrary.IActionResponse updateResponse = logic.Update(solution);
                    Assert.IsTrue(updateResponse.IsValid);
                }
                else if (GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasIntField))
                {
                    string intField = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.IntField);
                    int currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.Solution, int>(solution, intField);
                    int newValue = currentValue + 1;
                    KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.Solution>(solution, intField, newValue);
                    KnightsTour.CoreLibrary.IActionResponse updateResponse = logic.Update(solution);
                    Assert.IsTrue(updateResponse.IsValid);
                }
                #endregion
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Update_ Multiple.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_Update_Multiple()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                string pk = new KnightsTour.Solution().PrimaryKeyField;

                if (GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.RecordCount) > 0)
                {
                    List<KnightsTour.Solution> solutions = logic.GetAll(new KnightsTour.CoreLibrary.RetrievalCondition
                    {
                        SQLOrderBy = StorageProvider.GetColumnSQL(pk),
                        Skip = 0,
                        Take = BulkTestQuantity
                    }).ToList();

                    Assert.IsTrue(solutions.Count > 0);

                    // Modify each account.
                    bool modified = false;
                    foreach (KnightsTour.Solution solution in solutions)
                    {
                        if (GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasStringField))
                        {
                            string stringField = GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.StringField);
                            string currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.Solution, string>(solution, stringField);
                            string newValue = "modified";
                            if (string.IsNullOrEmpty(currentValue))
                            {
                                newValue = currentValue + " modified.";
                                if (currentValue.StartsWith("a"))
                                {
                                    newValue = currentValue.Replace("a", "z");
                                }
                            }
                            KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.Solution>(solution, stringField, newValue);
                            modified = true;
                        }
                        else if (GetEntityProperty<KnightsTour.Solution, KnightsTour.SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.HasIntField))
                        {
                            string intField = GetEntityProperty<KnightsTour.Solution, SolutionLite>(KnightsTour.CoreLibrary.Enumerations.EntityProperty.IntField);
                            int currentValue = KnightsTour.CoreLibrary.ReflectionAssistant.GetValue<KnightsTour.Solution, int>(solution, intField);
                            int newValue = currentValue + 1;
                            KnightsTour.CoreLibrary.ReflectionAssistant.SetValue<KnightsTour.Solution>(solution, intField, newValue);
                            modified = true;
                        }
                    }
                    if (modified)
                    {
                        KnightsTour.CoreLibrary.IActionResponse updateResponse = logic.Update(solutions);
                        Assert.IsTrue(updateResponse.IsValid);
                    }
                }
            }

        }

        /// <summary>
        /// Description for Solution Logic_ Delete By Id.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_DeleteById()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                #region Create and insert a new solution
                KnightsTour.Solution solution = InsertEntity<KnightsTour.Solution, KnightsTour.SolutionLite>();
                #endregion

                #region Now let's delete that newly created solution by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(solution.Id.Value);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Delete By Ids.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_DeleteByIds()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                #region Create and insert some new solutions
                List<int?> newIds = new List<int?>();
                for (int i = 0; i < BulkTestQuantity; i++)
                {
                    KnightsTour.Solution solution = InsertEntity<KnightsTour.Solution, KnightsTour.SolutionLite>();
                    newIds.Add(solution.Id.Value);
                }
                Assert.IsTrue(newIds.Count == BulkTestQuantity);
                #endregion

                #region Now let's delete that newly cretaed solution by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(newIds);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Delete By Entity.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_DeleteByEntity()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                #region Create and insert a new solution
                KnightsTour.Solution solution = InsertEntity<KnightsTour.Solution, KnightsTour.SolutionLite>();
                #endregion

                #region Now let's delete that newly created solution by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(solution);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Delete By Entities.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_DeleteByEntities()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                #region Create and insert some new solutions
                List<KnightsTour.Solution> newEntities = new List<KnightsTour.Solution>();
                for (int i = 0; i < BulkTestQuantity; i++)
                {
                    KnightsTour.Solution solution = InsertEntity<KnightsTour.Solution, KnightsTour.SolutionLite>();
                    newEntities.Add(solution);
                }
                Assert.IsTrue(newEntities.Count == BulkTestQuantity);
                #endregion

                #region Now let's delete that newly created solution by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.Delete(newEntities);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Delete_ Cascade.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_Delete_Cascade()
        {
            using (var transactionScope = new TransactionScope())
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);

                #region Create and insert a new solution
                KnightsTour.Solution solution = InsertEntity<KnightsTour.Solution, KnightsTour.SolutionLite>();
                #endregion

                #region Now let's delete that newly created solution by it's id
                KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.DeleteCascade(solution.Id.Value);
                Assert.IsTrue(deleteResponse.IsValid);
                #endregion
            }
        }

        /// <summary>
        /// Description for Solution Logic_ Delete_ By F K.
        /// </summary>
        [TestMethod]
        public void SolutionLogic_Delete_ByFK()
        {
            EntityMetaData meta = new EntityMetaData(KnightsTour.Enumerations.EntityName.Solution, UserName);

            if (meta.Dependencies.Count > 0)
            {
                Assert.Inconclusive("Delete by FK is not possible due to dependencies.");
            }
            else
            {
                KnightsTour.SolutionLogic logic = new KnightsTour.SolutionLogic(UserName);
                using (var transactionScope = new TransactionScope())
                {
                    foreach (string property in EntityMapper.FKNames(new KnightsTour.Solution().EntityName))
                    {
                        // Get some valid FK Values.
                        foreach (int fkValue in StorageProvider.GetFKValues<KnightsTour.Solution, KnightsTour.SolutionLite, int?>(property))
                        {
                            KnightsTour.CoreLibrary.IActionResponse deleteResponse = logic.DeleteByFK(property, fkValue);
                            Assert.IsTrue(deleteResponse.IsValid);
                        }
                    }
                }
            }
        }
        #endregion Solution logic tests Methods

    } // Class
} // Namespace