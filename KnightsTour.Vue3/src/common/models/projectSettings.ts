import { ProjectSettingsBase } from './projectSettingsBase';

/**
 * Class: ProjectSettings
 *
 * - A complete set of project settings.
 * - Overrite model settings here if you need to
 * - Or extend and add new project settings as required!
 *
 * @extends ProjectSettingsBase Base settings in sync with the model.
 */
export class ProjectSettings extends ProjectSettingsBase {
  constructor() {
    super();
  }
}
